# 2026-07-16

## 分析 skill-database 项目并补齐 AI 文档
- 项目：`/Users/admin/workspace/github/skill-database` —— 《选技大乱斗》非官方社区数据图鉴。
- 技术栈：Next.js 16 App Router + React 19 + Tailwind v4 + TS(strict)，`output:"export"` 纯静态导出（无后端/DB）。
- 架构：`src/lib/types.ts`(类型地基) → `src/lib/data.ts`(唯一数据入口，counters 单一数据源、counteredBy 派生) → `game-mechanics.ts`/`recommender.ts`(纯函数) → `components/*`。种子数据 `src/data/*`，带 `verified` 标记。
- 爬虫 `scripts/scraper` 独立子项目（tsx + DeepSeek LLM），被主 tsconfig exclude，只写 `draft/`（已 gitignore），**绝不**写 `src/data`。
- 当前无测试框架。
- 产出 AI 文档：`AGENTS.md`(权威，含架构/约定/红线)、`CLAUDE.md` 与 `.github/copilot-instructions.md`(轻量指针指向 AGENTS.md)。
